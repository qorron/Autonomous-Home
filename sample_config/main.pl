#!/usr/bin/perl
use strict;
use warnings;
use 5.020;
use Data::Dumper;

%config = (
	latitude           => 51.1789,
	longitude          => -1.8262,
	weather_cache_file => '/tmp/weather.storable',
);

